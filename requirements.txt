# Core AXIS Dependencies
torch>=2.4.0
numpy>=1.24.0
pillow>=10.0.0
pydantic>=2.0.0
pyyaml>=6.0
tqdm>=4.66.0
requests>=2.31.0
pytest>=8.0.0

# Optional VLM & Fine-tuning dependencies (for GPU experiments)
transformers>=4.45.0
peft>=0.14.0
accelerate>=0.30.0
bitsandbytes>=0.43.0; sys_platform != "win32"
