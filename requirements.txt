# Core AXIS Dependencies
torch>=2.4.0
numpy>=1.24.0
pillow>=10.0.0
pydantic>=2.0.0
pyyaml>=6.0
tqdm>=4.66.0
requests>=2.31.0
pytest>=8.0.0

# Dataset ingestion & preprocessing dependencies (required to import
# dataset_tools.preprocessing / dataset_tools.resplan and to collect
# tests/test_preprocessing.py and tests/test_phase3_scientific_readiness.py).
# These were previously missing from this file even though the code imports
# them unconditionally.
shapely>=2.0.0
pyarrow>=15.0.0
ifcopenshell>=0.7.0

# Optional VLM & Fine-tuning dependencies (for GPU experiments).
# These are loose bounds for general development, NOT the exact pins used to
# produce the reported VLM QLoRA proof-of-concept results. For bit-for-bit
# reproduction of that specific micro-experiment, use
# experiment_package/requirements.txt instead (see REPRODUCIBILITY.md).
transformers>=4.45.0
peft>=0.14.0
accelerate>=0.30.0
bitsandbytes>=0.43.0; sys_platform != "win32"
